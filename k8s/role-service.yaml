# k8s/role-service.yaml
# Define el despliegue y el servicio para el Role Service.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: role-service-deployment
  labels:
    app: role-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: role-service
  template:
    metadata:
      labels:
        app: role-service
    spec:
      containers:
      - name: role-service
        # IMPORTANTE: Reemplaza 'TU_ID_DE_PROYECTO' con tu ID de proyecto de Google Cloud.
        image: us-central1-docker.pkg.dev/himfg-476514/himfg-repo/role-service:v1
        ports:
        - containerPort: 8080
        env:
        # IMPORTANTE: Reemplaza 'IP_DE_TU_CLOUDSQL' por la IP privada de tu instancia de Cloud SQL.
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://10.11.192.3/micros_db"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: password
---
apiVersion: v1
kind: Service
metadata:
  name: role-service
spec:
  selector:
    app: role-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
